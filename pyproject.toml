[project]
name = "paw"
version = "0.1.0"
description = "A minimal AI agent"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "anthropic>=0.83.0",
    "think-llm>=0.0.11",
]

[project.scripts]
paw = "main:main_sync"
